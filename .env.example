# Base URL of the Tevexxo backend API (no trailing slash).
# Used by every page for services, products, posts, projects, stats,
# socials and the contact form. See src/lib/api.ts for the endpoint list.
VITE_API_BASE_URL=https://your-api-host.example.com
